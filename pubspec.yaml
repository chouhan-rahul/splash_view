name: splash_view
description: A splash view shows up when an application is loading.
version: 1.0.0
homepage: https://rahulchouhan.me
repository: https://github.com/imRahulChouhan/splash_view

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
